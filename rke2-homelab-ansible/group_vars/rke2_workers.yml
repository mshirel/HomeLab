---
# RKE2 Worker Node Configuration

# Node role
rke2_type: agent

# Storage configuration
setup_storage: true
storage_enabled: true

# Labels for worker nodes
# Note: Do not set node-role.kubernetes.io/worker - it prevents workers from joining
worker_node_labels:
  storage-node: "true"
