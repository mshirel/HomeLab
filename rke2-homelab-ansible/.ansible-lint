---
skip_list:
  - role-name[path]

profile: min

exclude_paths:
  - .git/
  - .github/
  - files/

# Tell ansible-lint about our role structure
mock_roles:
  - common
  - storage
  - rke2-master
  - rke2-worker
  - cilium
  - longhorn
  - traefik-ingress
  - test-apps
